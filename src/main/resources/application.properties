# Spring Application Name
spring.application.name=demo

# JPA Open In View
spring.jpa.open-in-view=false 
# Disables the OpenSessionInView filter

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/jdbc?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=bhattg
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update 
# Automatically create or update tables in the database
spring.jpa.show-sql=true 
# Show SQL queries in the console
spring.jpa.properties.hibernate.format_sql=true
# Format SQL queries for readability

# Enable the database initialization schema (optional)
spring.sql.init.mode=always 
# Ensure database schema is initialized on application startup

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mahadevbhatt4404@gmail.com
spring.mail.password=igeaclqmcyekzauq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Logging Configuration
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.security=DEBUG
